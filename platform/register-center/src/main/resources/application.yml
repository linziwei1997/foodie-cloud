## 启动顺序： #1

spring:
  application:
    name: eureka-servier
  profiles:
    active: dev

server:
  port: 20000

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  instance:
    ##将localhost指向本机(host文件)
    hostname: localhost